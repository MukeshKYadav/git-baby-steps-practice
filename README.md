# Python Calculator

A simple Python calculator project with addition, subtraction, and multiplication functions.

## Multiply Numbers

Use `multiply()` to return the product of two numbers:

```python
from calculator import multiply

result = multiply(10, 5)
print(result)  # 50
```

## Run

From this directory, run:

```powershell
python main.py
```